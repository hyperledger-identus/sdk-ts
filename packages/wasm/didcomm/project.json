{
  "projectType": "library",
  "tags": [
    "scope:building-block"
  ],
  "implicitDependencies": [
    "@hyperledger/identus-domain"
  ],
  "targets": {
    "externals": {
      "cache": true,
      "inputs": [
        "{workspaceRoot}/externals/didcomm/**/*",
        "{workspaceRoot}/externals/identus-didcomm.commit"
      ],
      "outputs": [
        "{projectRoot}/generated"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "./externals/run.sh -x build",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {},
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config=vitest.config.js --coverage",
        "cwd": "{projectRoot}"
      }
    },
    "test-watch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest watch --config=vitest.config.js",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "cache": true,
      "inputs": [
        "{projectRoot}/generated/**/*"
      ],
      "dependsOn": [
        "externals"
      ],
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/build"
      ],
      "options": {
        "command": "tsup",
        "cwd": "{projectRoot}"
      }
    }
  }
}